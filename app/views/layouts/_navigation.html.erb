<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="#"><%= fa_icon 'egg' %>Mchan</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
    </ul>
    <div class="btn-group" >
      <%= link_to 'Sign up', "", :class => "btn", data: {toggle: "modal", target: ".bd-sign-up-modal-lg"} %>
    
      <% if user_signed_in? %>
        <%= link_to "Logout", destroy_user_session_path, :class => "btn", method: :delete  %>
      <% else %>
        <%= link_to "Login", "", :class => "btn",data: {toggle: "modal", target: ".bd-sign-in-modal-lg"} %>
      <% end %>
    </div>
  </div>
</nav>
<%= render 'layouts/alerts' %>
<%= render 'layouts/sign_in_form'%>
<%= render 'layouts/sign_up_form'%>